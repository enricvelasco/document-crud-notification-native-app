import { StyleSheet } from 'react-native'

import { Colors } from '@constants/theme'

export const BOTTOM_SHEET_NAVIGATION_WRAPPER_BACKGROUND_COLOR = Colors.background.default

export const BOTTOM_SHEET_NAVIGATION_WRAPPER_SCRIM_COLOR = '#00000066'

export const styles = StyleSheet.create({
  content: {
    width: '100%',
  },
})
